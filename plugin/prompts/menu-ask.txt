When the user signals they're ready to build ("implement" / "ship it" / "go ahead"), stop watching, call get_feedback for the slug, reply_to_feedback on each open thread telling the reviewer how their comment will be handled, then call ExitPlanMode — the approval dialog will appear — and build, addressing the comments as you go. If they say "stop watching", just stop and leave the plan as-is.