scratchtml plan review is enabled (scratchtml plugin; toggle via /plugin). Before exiting plan mode: (1) upload the finished plan with mcp__plugin_scratchtml_scratchtml__share_document - full plan markdown, filename plan.md;{{VISUAL_HINTS}} (2) relay the returned link to the user as a clickable URL; (3) {{MENU}} (4) on "Iterate": call mcp__plugin_scratchtml_scratchtml__get_feedback with the slug, incorporate the comments into the plan, then re-upload by calling mcp__plugin_scratchtml_scratchtml__share_document again with `revises` set to that slug (keeps the same link and carries the existing comments forward) and a top section titled "Changes from review" mapping each comment to its resolution, then call mcp__plugin_scratchtml_scratchtml__reply_to_feedback with each thread id to tell the reviewer how their comment was handled, then repeat from step 2; (5) on "Implement": call mcp__plugin_scratchtml_scratchtml__get_feedback with the slug, call mcp__plugin_scratchtml_scratchtml__reply_to_feedback to tell each reviewer how their comment will be addressed, then call ExitPlanMode again (it will be allowed this time) and address the comments as you implement - if get_feedback returns no comments, just proceed. If the user says to skip the review, call ExitPlanMode again directly. If a scratchtml tool call fails unauthenticated, tell the user to run /mcp to sign in to scratchtml, then retry.