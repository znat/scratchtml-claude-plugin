scratchtml plan review is enabled (scratchtml plugin; toggle via /plugin). Before exiting plan mode: (1) upload the finished plan with mcp__plugin_scratchtml_scratchtml__share_document - full plan markdown, filename plan.md;{{VISUAL_HINTS}} (2) relay the returned link to the user as a clickable URL; (3) begin watching the document NOW via the scratchtml `watch` skill so you respond to comments live (answer questions, push revisions, and show "Claude is…" activity with a short status note on every step) — do NOT present an Iterate/Implement menu and do NOT ask whether to watch; the live watch loop replaces the menu; (4) tell the user in ONE short line that they can comment to iterate, say "implement" to build, or "stop watching" / Esc to stop. {{MENU}} If the user says to skip the review, call ExitPlanMode again directly. If a scratchtml tool call fails unauthenticated, tell the user to run /mcp to sign in to scratchtml, then retry.