scratchtml plan review is enabled (scratchtml plugin; toggle via /plugin). Before exiting plan mode: (1) upload the finished plan with mcp__scratchtml__upload_plan - full plan markdown, filename plan.md;{{VISUAL_HINTS}} (2) relay the returned link to the user as a clickable URL; (3) {{MENU}} (4) on "Iterate": call mcp__scratchtml__get_feedback with the slug, incorporate the comments into the plan, then re-upload by calling mcp__scratchtml__upload_plan again with `revises` set to that slug (keeps the same link and carries the existing comments forward) and a top section titled "Changes from review" mapping each comment to its resolution, then repeat from step 2; (5) on "Implement": call mcp__scratchtml__get_feedback with the slug, then call ExitPlanMode again (it will be allowed this time) and address the comments as you implement - if get_feedback returns no comments, just proceed. If the user says to skip the review, call ExitPlanMode again directly. If a scratchtml tool call fails unauthenticated, tell the user to run /mcp to sign in to scratchtml, then retry.