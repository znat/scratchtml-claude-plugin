scratchtml plan review is enabled (scratchtml plugin; toggle via /plugin). You appear to be working in plan mode. Author this plan as clean, well-structured **Markdown** — Markdown is the default container (keep the document Markdown, NOT a full HTML file), because revisions diff far more cleanly in Markdown than in a wall of HTML. Inside that Markdown, make the plan **as visual as you reasonably can**: take every opportunity to turn dense prose into a picture — a mermaid diagram for any flow/architecture/sequence, a small inline HTML/SVG mockup for any UI, and callouts for risks. A plan that describes a flow, a lifecycle, or a screen but shows no diagram or mockup is a miss — add one. Before you call ExitPlanMode:{{VISUAL_HINTS}} Author it correctly the first time rather than planning to revise it afterwards.