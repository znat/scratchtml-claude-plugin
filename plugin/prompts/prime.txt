scratchtml plan review is enabled (scratchtml plugin; toggle via /plugin). You appear to be working in plan mode. As you author this plan — before you call ExitPlanMode:{{VISUAL_HINTS}} Write the plan as clean, well-structured Markdown. When it's ready the plugin will have you upload it to scratchtml for inline review, so author it correctly the first time rather than planning to revise it afterwards.