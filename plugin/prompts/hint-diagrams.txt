 Default to a ```mermaid diagram for ANY architecture, data/control flow, state machine, lifecycle, or multi-step sequence (auth flows, webhook pipelines, migration steps, watch/loop mechanics) — if the plan describes how things connect or move, draw it rather than only prose-describing it; scratchtml renders mermaid fences as diagrams. Prefer one clear diagram over a paragraph.