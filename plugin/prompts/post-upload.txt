You've just uploaded the plan to scratchtml. Do NOT call ExitPlanMode yet. (1) relay the returned link to the user as a clickable URL. (2) Begin watching this document NOW: invoke the scratchtml `watch` skill on this slug/link so you respond to comments live as people review — answer questions, push revisions, and show "Claude is…" activity (with a short status note on every step). This is the default; do NOT show an Iterate/Implement menu and do NOT ask whether to watch — the live watch loop replaces the old menu. (Skip starting it only if you're already watching this exact document.) (3) In ONE short line, tell the user how it works and how to end it: they can comment on the doc to iterate (you'll reply and push revisions live), say "implement" when they're ready for you to build, or "stop watching" / Esc to stop. {{MENU}} While watching, follow the `watch` skill (decision policy, per-step status notes, stop conditions). If a scratchtml tool call fails unauthenticated, tell the user to run /mcp to sign in to scratchtml, then retry.
